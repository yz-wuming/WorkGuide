apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "workguide.fullname" . }}-extensions
  namespace: {{ include "workguide.namespace" . }}
  labels:
    {{- include "workguide.labels" . | nindent 4 }}
data:
  extensions_config.json: |
{{ .Values.extensionsConfig | default "" | indent 4 }}
