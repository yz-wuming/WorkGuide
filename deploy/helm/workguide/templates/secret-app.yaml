{{- if not .Values.existingAppSecret -}}
{{- $prev := lookup "v1" "Secret" (include "workguide.namespace" .) (include "workguide.appSecret" .) -}}
{{- $betterAuth := "" -}}
{{- $internalToken := "" -}}
{{- if $prev -}}
{{- $betterAuth = index $prev.data "BETTER_AUTH_SECRET" | default "" | b64dec -}}
{{- $internalToken = index $prev.data "WORKGUIDE_INTERNAL_AUTH_TOKEN" | default "" | b64dec -}}
{{- end -}}
{{- if not $betterAuth -}}{{- $betterAuth = randAlphaNum 48 | lower -}}{{- end -}}
{{- if not $internalToken -}}{{- $internalToken = randAlphaNum 40 -}}{{- end -}}
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "workguide.appSecret" . }}
  namespace: {{ include "workguide.namespace" . }}
  labels:
    {{- include "workguide.labels" . | nindent 4 }}
type: Opaque
stringData:
  BETTER_AUTH_SECRET: {{ $betterAuth | quote }}
  WORKGUIDE_INTERNAL_AUTH_TOKEN: {{ $internalToken | quote }}
{{- end -}}
