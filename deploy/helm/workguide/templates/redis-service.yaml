{{- if .Values.redis.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "workguide.redisFullname" . }}
  namespace: {{ include "workguide.namespace" . }}
  labels:
    {{- include "workguide.labels" . | nindent 4 }}
    app.kubernetes.io/component: redis
spec:
  type: ClusterIP
  ports:
    - name: tcp-redis
      port: 6379
      targetPort: tcp-redis
  selector:
    {{- include "workguide.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: redis
{{- end -}}
