apiVersion: v1
kind: Service
metadata:
  name: frontend
  namespace: {{ include "workguide.namespace" . }}
  labels:
    {{- include "workguide.labels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3000
      targetPort: http
  selector:
    {{- include "workguide.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: frontend
