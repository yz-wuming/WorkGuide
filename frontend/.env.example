# WorkGuide 前端环境变量模板（Vite）
# ─────────────────────────────────────────────────────────
# 重要：当前前端是纯静态 SPA，frontend/src 中【不引用任何 VITE_* 变量】。
# 因此本文件当前不需要填写任何值，也请不要把任何 API Key / Token / Password
# 填到这里——Vite 的 VITE_* 变量会被编译进浏览器可见的 bundle，属于公开内容。
#
# 复制为 frontend/.env 是可选的。若存在，仅用于向后兼容；缺少该文件时
# docker compose 也会正常启动（compose 中已标记 required: false）。
#
# 需要前端运行时配置时，只在这里定义某个变量，再在代码中用
# import.meta.env.VITE_XXX 读取，并同步更新本文件。
# VITE_XXX=